'''
barebones config of swapping station lift stuff, similar to config.Made with intent to have other potential smart cart capabilities
'''

from enum import Enum

class Motors(Enum):
    Lift_Screw = 30 #arbitrarily larger than other motor id's
    
    
